import React from 'react'
import { Button } from '@mui/material'

import styles from './Quantity2.module.scss'

const Quantity2 = ({ qty, incQty, decQty, itemQuantity } : { qty: number, incQty: any, decQty: any, itemQuantity: number }) => {
  return (
    <div className= {styles.container}>
      
    </div>
  )
}

export default Quantity2